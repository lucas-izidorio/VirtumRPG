<?xml version="1.0" encoding="utf-8" ?>
<Application
    x:Class="Virtum.App"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Virtum.Controls;assembly=Virtum">
    <Application.Resources>
        <Color x:Key="PrimaryColor">#CE4F00</Color>
        <Color x:Key="PrimaryColorTransparent">#AACE4F00</Color>
        <Color x:Key="PlainWhite">#FFFFFF</Color>
        <Color x:Key="LightGray">#CECECE</Color>

        <Style TargetType="NavigationPage">
            <Setter Property="BarBackgroundColor" Value="{StaticResource PrimaryColor}" />
            <Setter Property="BarTextColor" Value="{StaticResource PlainWhite}" />
        </Style>

        <Style x:Key="ImageEntryStyle" TargetType="local:ImageEntry">
            <Setter Property="HeightRequest" Value="50" />
            <Setter Property="LineColor" Value="{StaticResource LightGray}" />
            <Setter Property="PlaceholderColor" Value="{StaticResource PrimaryColorTransparent}" />
            <Setter Property="TextColor" Value="{StaticResource PrimaryColor}" />
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="TextColor" Value="{StaticResource PlainWhite}" />
            <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}" />
        </Style>

    </Application.Resources>
</Application>